<section class="form">
  <%= form_for [:admin,@world] do |f| %>
    <% if @world.errors.any? %>
    <div class="errorExplanation">
      <h2><%= pluralize(@world.errors.count, "error") %> prohibited this world from being saved:</h2>
      <ul>
      <% @world.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
    <% end %>
    <table width="550">
      <tr>
        <td class="fieldLabel">
          <%= f.label :name %>
        </td>
        <td class="fieldCell">
          <%= f.text_field :name %>
        </td>
      </tr>
    </table>
    <br/>
    <%= f.submit %>
  <% end %>
</section>
