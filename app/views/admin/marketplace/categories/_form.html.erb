<%= form_for([:admin, @category]) do |f| %>
  <% if @category.errors.any? %>
    <div class="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name, 'Category Name' %><br>
  <%= f.text_field :name %><br>
  <%= f.label :marketplace_theme_id, 'Theme' %><br>
  <%= f.select :marketplace_theme_id, options_from_collection_for_select(MarketplaceTheme.all, 'id', 'name', @category.marketplace_theme_id), :include_blank => true %>
  <%= f.submit 'Save' %>
<% end %>
